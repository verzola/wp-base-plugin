{
  "private": true,
  "name": "wp-base-plugin",
  "version": "0.0.1",
  "scripts": {
    "format": "prettier --write \"**/*.{js,json,scss,html,md}\"",
    "lint": "npm run -s lint:js && npm run -s lint:scss",
    "lint:js": "eslint resources/assets/js/**/*.js",
    "lint:scss": "stylelint \"resources/assets/scss/**/*.scss\"",
    "build": "cross-env NODE_ENV=production webpack --progress --hide-modules --env.production",
    "start": "cross-env NODE_ENV=development webpack --watch --progress --hide-modules --env.development",
    "test": "npm run -s lint"
  },
  "author": "_author",
  "devDependencies": {
    "@verzola/bass": "^0.0.13",
    "husky": "^1.2.0",
    "lint-staged": "^8.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ],
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ],
    "*.php": [
      "./vendor/bin/php-cs-fixer fix'",
      "git add"
    ]
  },
  "browserslist": [
    "> 1%",
    "last 2 version",
    "IE 10"
  ]
}
